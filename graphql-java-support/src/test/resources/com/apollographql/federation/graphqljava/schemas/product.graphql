schema {
  query: Query
}

type Money {
  amount: Int!
  currencyCode: String! @deprecated(reason : "dummy")
}

type Product @key(fields : "upc") {
  """
  The product "name".
  """
  name: String
  "Price in pennies"
  price: Int
  sku: String!
  upc: String!
}

type Query
